---
const item = Astro.props;
---


<div class="flex flex-col items-center">
                <img src={item.icon} alt={item.name} title={item.name} class="w-16 h-16 mb-2 grayscale" />
                <!-- <p class="text-sm text-center">{item.name}</p> -->
            </div>